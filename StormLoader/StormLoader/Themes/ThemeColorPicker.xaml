<UserControl x:Class="StormLoader.Themes.ThemeColorPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:StormLoader.Themes"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="800">
    <Border Height="auto" HorizontalAlignment="Stretch" VerticalAlignment="Top" DockPanel.Dock="Top" BorderThickness="2 2 2 2" BorderBrush="{DynamicResource border-color}" CornerRadius="5" Margin="10">
        <Grid >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Style="{DynamicResource MaterialDesignFloatingActionButton}" Name="ColorBtn" Click="ColorBtn_Click" Margin="10" Grid.Column="0" materialDesign:ShadowAssist.ShadowDepth="Depth2" Height="25" Width="25">
                
            </Button>
            <Label DockPanel.Dock="left" Width="auto"   Name="ColorLabel"  Margin="10" BorderThickness="0,0,0,0"   FontSize="20"  HorizontalAlignment="Stretch" Padding="5" Grid.Column="1"/>
        </Grid>

    </Border>
</UserControl>
